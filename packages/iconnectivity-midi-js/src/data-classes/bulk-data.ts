/**
 * This data class is used to perform bulk data transfer operations (i.e. backup and restore) between a
 * device and a host (or between two devices where one device acts as a host).
 */
export enum BulkData {}
